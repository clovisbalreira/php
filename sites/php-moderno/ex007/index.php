<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Operadores aritméticos, expressões aritméticas</title>
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <main>
        <!-- 
            . = operator de contetanação
            + = operador aritmetico 
    
            operadores
            + = adiçao
            - = subtração
            * = multiplicação
            / = divisão 
            % = modulo da divisão inteira ( resto )
            ** = potencia
            
            Expressões aritmeticas 
            1º ()
            2º **
            3º * - / - % ( sempre resolve primeiro da esquerda para direita )
            4º + - - ( sempre resolve primeiro da esquerda para direita )

            Funções matematícas
            abs(valor) = valor absoluto retira o sinal do valor
            base_convert(valor, valor da base, valor da base para converter) = converte valores pra outra base
            ceil(valor) = arrendonda pra cima
            floor(valor) = arrendonda pra baixo
            round(valor) = arrendonda pra numero mais proximo
            hypot(valor) = calcula a hipotenusa
            intdiv(valor) = divisão inteira
            min(valores) = valor minimo de uma sequencia 
            max(valores) = valor maximo de uma sequencia 
            pi() ou M_PI= valor de pi
            pow(valor, potencia) = valor de potencia // perte a ordem de procetencia quanto usa a função
            sin() = calcular seno 
            cos() = calcular cosseno
            tan() = calcula tangente
            sqrt() = calcular a raiz quadrada
            numero ** (1/3) = calcular raiz cubica 
        -->
    </main>    
</body>
</html>